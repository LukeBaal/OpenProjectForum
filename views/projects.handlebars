<h1>All Projects</h1>

{{#each project}}
<div class="card">
  <div class="card-body">
    <h1>{{title}}</h1>
    <h3>GitHub: {{github}}</h3>
    <h3>Author: {{author_id}}</h3>
    <p>{{description}}</p>
  </div>
</div>
{{/each}}